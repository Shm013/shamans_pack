require("defines")

data:extend
{
	{
		type = "bool-setting",
		name = wagon_color_defines.names.setting.also_paste_color,
		setting_type = "runtime-per-user",
		default_value = true,
		order = "a"
	}
}